<div id ="conversations">

<div class=”ui segment”>
  <h1 class="conversations_heading">Chats</h1>

  <div class="conversations_body">

  <div class=”ui list”>
    <div class=”item”>
      <% @conversations.each do |conversation| %>
        <% if conversation.sender_id == current_user.id ||      conversation.recipient_id == current_user.id %>

          <% if conversation.sender_id == current_user.id %>
            <% recipient = Account.find(conversation.recipient_id) %>
          <% else %>
            <% recipient = Account.find(conversation.sender_id) %>
          <% end %>

        <%= link_to recipient.name, conversation_messages_path(conversation), class: 'buddy_name'%><br>
       <% end %>
     <% end %>
   </div>
 </div>
</div>
</div>

</div>
<p><%= button_to 'Back', accounts_path(@account), class: 'back_to_chats', method: :get %></p>



</div>
